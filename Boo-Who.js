function booWho(bool) {
    if(bool === true || bool === false ){
        return true;
    }
    else{
        return false;
    }
    // return bool;
  }
  console.log(booWho(false));
//   booWho(null);